<?php
    //array_filter
    //array_map
    //array_column
    //sprintf — Return a formatted string (can insert varaibles as well)
    //explode — Split a string by string
    //implode — Join array elements with a string
    //... operator(splat operator) Arrays and Traversable objects can be unpacked into argument lists when calling functions by using this operator
    //compact — Create array containing variables and their values
    //extract — Import variables into the current symbol table from an array
    //header  — Send a raw HTTP header
 ?>
